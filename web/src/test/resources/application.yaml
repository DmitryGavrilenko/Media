spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/images
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 8
  jpa:
#    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    properties:
      hibernate:
        jdbc:
          lob.non_contextual_creation: true
  profiles:
    active: ${prfile:test}
server:
  port: 9000

server_url: http://localhost:9000/image/upload

image_path: ${IMAGE_PATH:/home/dmitry/IdeaProjects/Media/images/}
---
spring:
  profiles: verification
  datasource:
    username: viktor
    password: 123456

---
 spring:
  profiles: working
  datasource:
    username: postgres
    password: root

---
 spring:
  profiles: den
  datasource:
    username: root
    password: root
---
spring:
  profiles: test
  datasource:
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/images_test
server:
  port: 9001